import React from 'react';
import './HomePage.scss';
import { connect } from 'react-redux';

const HomePage = () => {
  return <div className="home-page">View Campaign Performance</div>;
};

export default connect(null, {})(HomePage);
